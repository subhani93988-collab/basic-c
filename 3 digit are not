#include <stdio.h>
int main() {
    int n;
    printf("enter the number:");
    scanf("%d",&n);
    if(n>=100&&n<=999)
    printf("three digit numbers");
    else
    printf("not a three digit number");
    
    return 0;
}
